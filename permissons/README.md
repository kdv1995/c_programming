- regular file flag
-d directory flag;

we have three permissions groups:
- user (u)-> wrx -> permissions mode
- group (g)
- others (o)
Check File Existence and Permissions

Use access() or stat() to check if a file exists and whether it’s readable/writable.


File Metadata Viewer

Display metadata like file size, permissions, last modified time using stat().
